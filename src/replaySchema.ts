/**
 * @hidden
 * Supported OOProc DF extension protocols
 */
export enum UpperSchemaVersion {
    V1 = 0,
    V2 = 1,
}

/**
 * @hidden
 * The support DF extension OOProc replay schemas supported by this SDK
 */
export enum ReplaySchema {
    V1,
    V2,
}
